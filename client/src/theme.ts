import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#17202e",
      light: "skyblue",
    },
    secondary: {
      main: "#0abeff",
    },
    error: {
      main: "#rttr",
    },
  },
});
